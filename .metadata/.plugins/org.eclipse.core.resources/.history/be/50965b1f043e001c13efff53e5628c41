create or replace procedure javatest(
  p1 in varchar2,
  p2 in out varchar2,
  p3 out varchar2) 
 is
begin
IF ()THEN
	a_exponent := 0; 
 END IF;
	a_power := 1;
	for  i IN 1..a_exponent  LOOP
	a_power := a_power*a_num;
	END LOOP;


end;
